import {loadLibSource} from '@/utils/fsUtils';
import {CodeBlock} from '@/shared-components/CodeBlock';

# TreeMultiSelect API

API docs for the `TreeMultiSelect` component.

## Types

### Core
<CodeBlock code={loadLibSource('types/core.ts')}/>

### TreeNode
<CodeBlock code={loadLibSource('types/nodes.ts')}/>

### Configs
<CodeBlock code={loadLibSource('types/configs.ts')}/>

### Virtual focus
<CodeBlock code={loadLibSource('types/virtualFocus.ts')}/>

### Components
<CodeBlock code={loadLibSource('types/components.ts')}/>

### Internal state
<CodeBlock code={loadLibSource('types/state.ts')}/>

## Props
<CodeBlock code={loadLibSource('types/props.ts')}/>

## Imperative API
<CodeBlock code={loadLibSource('types/api.ts')}/>
