{
  "name": "react-tree-multi-select-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "start": "turbo run start",
    "build-lib": "turbo run build --filter=react-tree-multi-select",
    "build": "turbo run build"
  },
  "packageManager": "npm@10.9.3"
}
